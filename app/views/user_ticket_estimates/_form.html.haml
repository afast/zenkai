= form_for @user_ticket_estimate do |f|
  - if @user_ticket_estimate.errors.any?
    #error_explanation
      %h2= "#{pluralize(@user_ticket_estimate.errors.count, "error")} prohibited this user_ticket_estimate from being saved:"
      %ul
        - @user_ticket_estimate.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
