= form_for @ticket, remote: true do |f|
  - if @ticket.errors.any?
    #error_explanation
      %h2= "#{pluralize(@ticket.errors.count, "error")} prohibited this ticket from being saved:"
      %ul
        - @ticket.errors.full_messages.each do |msg|
          %li= msg

  .form-group
    = f.text_field :name, class: 'form-control', placeholder: 'Ticket'
  .form-group
    = f.number_field :points, class: 'form-control', placeholder: 'Estimate'
  .form-group
    = f.select :project_id, options_from_collection_for_select(@projects, :id, :abbreviation),{:class => 'form-control'}
  .form-group
    = f.hidden_field :user_id, value: current_user.id
  .actions
    = f.submit 'Save', class: 'btn btn-success'
